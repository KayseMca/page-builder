import { SeachPagePipe } from './seach-page.pipe';

describe('SeachPagePipe', () => {
  it('create an instance', () => {
    const pipe = new SeachPagePipe();
    expect(pipe).toBeTruthy();
  });
});
